client:
  web_ui_port: 8080
  socks5_port: 1080
  data_dir: /app/data

tunnel:
  jitter_max_ms: 500   # случайная задержка 0–500ms перед каждым подключением
  sni_list:            # SNI подставляется случайно из списка (маскировка под реальные домены)
    - "photo.petroff.pro"
    - "www.petroff.pro"

balancing:
  strategy: round-robin  # round-robin, latency, least-loaded
  health_check_interval: 30s
  failover_enabled: true

servers:
  # Example servers (initially empty)
  # - id: server1
  #   host: example.com
  #   port: 443
  #   enabled: true
